allOf:
  - $ref: "#/components/schemas/PlatformKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - chain_identifier
          - name
          - shortname
        properties:
          chain_identifier:
            type: integer
            example: 10
          name:
            type: string
            example: Optimistic Ethereum
          shortname:
            type: string
            example: Optimistic Ethereum
